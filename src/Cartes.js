import React from 'react';
import HomeMenu from './Components/HomeMenu';

const Cartes = () => {
    window.scrollTo(0, 0);
    return (
        <>
            <HomeMenu/>
        </>
    );
};

export default Cartes;